server:
  shutdown: immediate
  port: 8084

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

api:
  base:
    url:
      oauth: http://localhost:9090/auth

spring:
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/dev

  devtools:
    add-properties: true

  datasource:
    url: 'jdbc:h2:mem:mapping-db;MODE=PostgreSQL'

  jpa:
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console
